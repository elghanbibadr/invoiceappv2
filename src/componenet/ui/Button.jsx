import React from 'react'

export const Button = ({ className }) => {
    return (
        <button className={`${className} rounded-full text-[0.8rem]  leading-[0.93rem] capitalize text-white font-bold  py-[1rem] px-[1.5rem]`}>delete</button>
    )
}
